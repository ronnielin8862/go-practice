version: '3'

includes:
  addDigits: ./cmd/algorithm/addDigits/

tasks:
  run:
    cmds:
      - cmd: echo '111'
        ignore_error: true
      - task: addDigits:test
#      - cmd: go run .
#        ignore_error: true
    silent: false
    dir: ./
  helper:
    - cmd: sleep 2 ; echo '222'
  test:
    deps: [helper, run]

